import { StringPrefixPipe } from './string-prefix.pipe';

describe('StringPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new StringPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
